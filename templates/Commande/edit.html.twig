{# templates/commande/edit.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Edit Commande{% endblock %}

{% block body %}
    <h1>Edit Commande</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}  {# This renders all the form fields.  You could use form_row() for more control. #}
        <button type="submit" class="btn btn-primary">Save Changes</button>
    {{ form_end(edit_form) }}

    <a href="{{ path('commande_index') }}" class="btn btn-secondary">Back to list</a>
    {{ include('commande/_delete_form.html.twig') }} {# Include the delete form #}
{% endblock %}