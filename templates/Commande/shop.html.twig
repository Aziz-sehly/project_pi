{# templates/commande/shop.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Shop{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center mb-4">Our Products</h1>

    <div class="row">
        {% for product in products %}
            <div class="col-md-4 mb-4">
                <div class="card">
                    <img src="{{ asset('images/product_placeholder.png') }}" class="card-img-top" alt="Product Image">
                    <div class="card-body">
                        <h5 class="card-title">{{ product.name }}</h5>
                        <p class="card-text">{{ product.description }}</p>
                        <p><strong>Price:</strong> €{{ product.price }}</p>
                        <a href="{{ path('app_commande_new', {'id': product.id}) }}" class="btn btn-primary">Order</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
