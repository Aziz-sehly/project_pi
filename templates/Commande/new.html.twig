{# templates/commande/new.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Create Commande{% endblock %}

{% block body %}
    <h1>Create Commande</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}  {# Render all form fields (easier than form_row) #}
        <button type="submit" class="btn btn-success">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('commande_index') }}" class="btn btn-secondary">Back to list</a>
{% endblock %}